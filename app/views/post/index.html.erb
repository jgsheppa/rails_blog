<h1>My blog<h1>

<% @posts.each do |post| %>
  <div>
   <h2><%= link_to post.title, post %></h2>
    <%= post.body %>
  </div>
<% end %>

<% if user_signed_in? %>
  <%= link_to "New Post", :new_post %>
<%end %>